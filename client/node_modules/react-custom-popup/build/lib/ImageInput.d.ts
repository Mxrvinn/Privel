import React, { Dispatch, SetStateAction } from 'react';
import { InputProps } from "./index";
interface IProps {
    setInputValues: Dispatch<SetStateAction<{
        [key: string]: any;
    }>>;
    inputValues: {
        [key: string]: any;
    };
    item: InputProps;
    error?: string;
    errorMessageStyle?: React.CSSProperties;
}
export default function ImageInput({ setInputValues, inputValues, item, error, errorMessageStyle }: IProps): JSX.Element;
export {};
