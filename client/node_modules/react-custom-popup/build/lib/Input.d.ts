import React, { Dispatch, SetStateAction } from 'react';
import { InputProps } from "./index";
interface IProps {
    item: InputProps;
    setInputValues: Dispatch<SetStateAction<{
        [key: string]: any;
    }>>;
    inputValues: {};
    error?: string;
    errorMessageStyle?: React.CSSProperties;
}
export default function Input({ item, setInputValues, inputValues, error, errorMessageStyle }: IProps): JSX.Element;
export {};
